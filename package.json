{
  "name": "avax-evm-starter",
  "version": "0.0.1",
  "description": "an evm starter project index pangolin transfer",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Ian He & Jay Ji",
  "license": "MIT",
  "dependencies": {
    "@pangolindex/exchange-contracts": "^1.0.2"
  },
  "devDependencies": {
    "@polkadot/api": "7.7.1",
    "@terra-money/terra.js": "^3.0.8",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "4.5.5"
  }
}
